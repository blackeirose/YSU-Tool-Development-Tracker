# Tracker six-digit Email OTP — validation record

Status: reviewed candidate; real production OTP acceptance pending.

Scope: apply MAIN-style six-digit OTP to Tracker, preserve table/card views, filters, links, owner editing, public read-only access, local cache, tracker_items RLS, GitHub Pages and current data. No shared SMTP/Auth configuration changes, database writes/migrations or other-app code changes.

Recovery baseline: e64d16209f1a9ad6243f38ed13d0a109fd09ea51. Before implementation, production index.html, app.js, owner-ui.js and owner-ui.css matched that source after line-ending normalization. Roll back frontend feature changes through Git revert and the existing main-branch Pages deployment; the current shared Token + ConfirmationURL template remains compatible with the old Magic Link flow. No database rollback is required.

Implementation: reviewed MAIN OTP component reused as a classic script; pinned/vendored Supabase SDK 2.116.0. Single guarded Auth lifecycle replaces duplicate writers. Exact owner UUID 38531f7e-e05e-473a-a587-500b1d3aebe5, confirmed non-anonymous user required. Hidden legacy UI routes to the same no-signup OTP flow. Logout revokes editing before network and guards stale mutation handlers.

Verification:
- Seven local tests PASS: guest read-only, six-digit request contract/leading-zero paste/owner editing, stale hydration, immediate logout/late verification, non-owner local fallback, invalid/expired/network/rate-limit/cooldown, session restoration and delayed guest hydration.
- Local browser fixture PASS at 390/820/1440: dialog fits, email 16px, OTP 24px, primary buttons 48px, invalid-code feedback, Enter success, owner controls, reload restoration and logout. Zero console errors. This fixture sends no email and uses no production data.
- Independent review PASS after fixing delayed guest hydration closing an already-open form.
- Read-only production policy inspection confirms tracker_items public SELECT and exact owner UUID INSERT/UPDATE/DELETE (UPDATE has USING and WITH CHECK). No policy changed.
- Real production delivery/session/edit acceptance remains pending after candidate deployment. No automated test email sent in this task.

No formal Tracker GitHub releases existed at the start; this change follows its existing commit-based deployment convention. Do not invent a semantic version or change the MAIN Registry version without a real version source.
